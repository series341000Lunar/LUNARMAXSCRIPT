# Repository Guidelines

- Each MAXScript tool must live in its own top-level directory.
- Shared repository files remain at the repository root.
- Do not modify other tool directories unless explicitly requested.
- Preserve working runtime behavior during repository reorganization.
- Keep tool-specific documentation in each tool directory.
